{
  "name": "ai-growth-agentic-commerce",
  "version": "1.0.0",
  "description": "AI Growth & Agentic Commerce Platform with Autonomous Multi-Agent Orchestrator",
  "scripts": {
    "dev": "concurrently -n \"SERVER,CLIENT\" -c \"cyan,magenta\" \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "build:server": "npm --prefix server run build",
    "build:client": "npm --prefix client run build",
    "build": "npm --prefix server run build && npm --prefix client run build",
    "seed": "npm --prefix server run prisma:seed"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
